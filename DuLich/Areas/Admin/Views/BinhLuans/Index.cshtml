@model IEnumerable<DuLich.Models.EF.BinhLuan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BanTin.TieuDe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NguoiDung.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NoiDung)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BanTin.TieuDe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NguoiDung.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NoiDung)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDBinhLuan }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDBinhLuan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDBinhLuan })
        </td>
    </tr>
}

</table>
